import common from '~/locales/en/common.json';
import cta from '~/locales/en/cta.json';
import footer from '~/locales/en/footer.json';

const resources = {
  common,
  cta,
  footer,
} as const;

export default resources;
